# Técnicos — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Cadastrar técnicos no painel, reservar rastreadores do estoque pra eles em lote, e deixar o técnico finalizar a instalação sozinho pelo celular em `/tecnico`.

**Architecture:** `Technician` é um model próprio com auth própria (JWT `type: 'technician'`), espelhando o app do associado já em produção. A reserva são três campos escalares em `StockItem` — um equipamento nunca fica em dois logins. A finalização em campo chama o mesmo `StockService.associate()` que o painel usa hoje, então a validação SGA não é duplicada.

**Tech Stack:** NestJS 11 + Prisma + PostgreSQL 17 + bcrypt + @nestjs/jwt no backend; Next.js 16 (App Router) + React 19 + Tailwind + shadcn/ui + axios + sonner no frontend.

## Global Constraints

- **Multi-tenant:** toda query filtra por `tenantId`. Sem exceção, inclusive `findFirst`/`findUnique`.
- **Soft delete:** `deletedAt`, nunca `delete()` físico.
- **Imports do Prisma Client:** `.prisma/client`, nunca `@prisma/client`.
- **Roles em inglês, UI em PT-BR.** Rotas frontend em PT-BR (`/tecnicos`, `/tecnico`), rotas backend em inglês (`/technicians`, `/tech`).
- **Migração em produção é aditiva** (`IF NOT EXISTS`, colunas nullable ou com default). Nunca `prisma db push` contra produção.
- **Nunca commitar segredos.** Senha provisória nunca é logada nem persistida em claro.
- **Commits em português:** `tipo(escopo): descrição`.
- **Build no droplet é sequencial** — nunca buildar backend e frontend ao mesmo tempo (OOM derruba containers).
- **Deploy exige push pro registry interno** `localhost:5000` — só `docker build` + `service update` não basta.

---

### Task 1: Schema — model Technician e campos de reserva

**Files:**
- Modify: `backend/prisma/schema.prisma`
- Create: `backend/prisma/migrations/20260721120000_add_technicians/migration.sql`

**Interfaces:**
- Produces: model `Technician`; `StockItem.assignedTechnicianId | assignedAt | assignedById`; `Device.installedByTechnicianId`.

- [ ] **Step 1: Adicionar o model Technician ao schema**

Em `backend/prisma/schema.prisma`, logo após o model `Associate` (linha ~414):

```prisma
/// Técnico instalador. Auth própria por CPF + senha (JWT type: 'technician'),
/// isolada do User do painel e do Associate do app — técnico nunca acessa o painel.
model Technician {
  id                  String    @id @default(uuid()) @db.Uuid
  name                String
  cpf                 String
  phone               String?
  email               String?
  canReceiveEquipment Boolean   @default(true) @map("can_receive_equipment")
  active              Boolean   @default(true)
  password            String?
  mustChangePassword  Boolean   @default(true) @map("must_change_password")
  lastLoginAt         DateTime? @map("last_login_at")
  tenantId            String    @map("tenant_id") @db.Uuid
  deletedAt           DateTime? @map("deleted_at")
  createdAt           DateTime  @default(now()) @map("created_at")
  updatedAt           DateTime  @updatedAt @map("updated_at")

  tenant     Tenant      @relation(fields: [tenantId], references: [id])
  stockItems StockItem[]
  devices    Device[]

  @@unique([tenantId, cpf])
  @@index([tenantId, deletedAt])
  @@map("technicians")
}
```

- [ ] **Step 2: Adicionar os campos de reserva em StockItem**

No model `StockItem`, depois de `deviceId`:

```prisma
  // Reserva pro login do técnico. Escalar (não N:N) porque um equipamento
  // nunca pode estar em dois logins ao mesmo tempo.
  assignedTechnicianId String?     @map("assigned_technician_id") @db.Uuid
  assignedTechnician   Technician? @relation(fields: [assignedTechnicianId], references: [id])
  assignedAt           DateTime?   @map("assigned_at")
  assignedById         String?     @map("assigned_by_id") @db.Uuid
```

E o índice, junto dos outros `@@index` do model:

```prisma
  @@index([tenantId, assignedTechnicianId])
```

- [ ] **Step 3: Adicionar o vínculo em Device**

No model `Device`, junto de `installedBy`:

```prisma
  installedByTechnicianId String?     @map("installed_by_technician_id") @db.Uuid
  installedByTechnician   Technician? @relation(fields: [installedByTechnicianId], references: [id])
```

Adicionar também `technicians Technician[]` no model `Tenant`, junto das outras relações.

- [ ] **Step 4: Escrever a migration SQL manualmente (aditiva)**

Criar `backend/prisma/migrations/20260721120000_add_technicians/migration.sql`:

```sql
CREATE TABLE IF NOT EXISTS "technicians" (
    "id" UUID NOT NULL,
    "name" TEXT NOT NULL,
    "cpf" TEXT NOT NULL,
    "phone" TEXT,
    "email" TEXT,
    "can_receive_equipment" BOOLEAN NOT NULL DEFAULT true,
    "active" BOOLEAN NOT NULL DEFAULT true,
    "password" TEXT,
    "must_change_password" BOOLEAN NOT NULL DEFAULT true,
    "last_login_at" TIMESTAMP(3),
    "tenant_id" UUID NOT NULL,
    "deleted_at" TIMESTAMP(3),
    "created_at" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updated_at" TIMESTAMP(3) NOT NULL,
    CONSTRAINT "technicians_pkey" PRIMARY KEY ("id")
);

CREATE UNIQUE INDEX IF NOT EXISTS "technicians_tenant_id_cpf_key" ON "technicians"("tenant_id", "cpf");
CREATE INDEX IF NOT EXISTS "technicians_tenant_id_deleted_at_idx" ON "technicians"("tenant_id", "deleted_at");

ALTER TABLE "technicians" DROP CONSTRAINT IF EXISTS "technicians_tenant_id_fkey";
ALTER TABLE "technicians" ADD CONSTRAINT "technicians_tenant_id_fkey"
    FOREIGN KEY ("tenant_id") REFERENCES "tenants"("id") ON DELETE RESTRICT ON UPDATE CASCADE;

ALTER TABLE "stock_items" ADD COLUMN IF NOT EXISTS "assigned_technician_id" UUID;
ALTER TABLE "stock_items" ADD COLUMN IF NOT EXISTS "assigned_at" TIMESTAMP(3);
ALTER TABLE "stock_items" ADD COLUMN IF NOT EXISTS "assigned_by_id" UUID;
CREATE INDEX IF NOT EXISTS "stock_items_tenant_id_assigned_technician_id_idx"
    ON "stock_items"("tenant_id", "assigned_technician_id");

ALTER TABLE "stock_items" DROP CONSTRAINT IF EXISTS "stock_items_assigned_technician_id_fkey";
ALTER TABLE "stock_items" ADD CONSTRAINT "stock_items_assigned_technician_id_fkey"
    FOREIGN KEY ("assigned_technician_id") REFERENCES "technicians"("id") ON DELETE SET NULL ON UPDATE CASCADE;

ALTER TABLE "devices" ADD COLUMN IF NOT EXISTS "installed_by_technician_id" UUID;
ALTER TABLE "devices" DROP CONSTRAINT IF EXISTS "devices_installed_by_technician_id_fkey";
ALTER TABLE "devices" ADD CONSTRAINT "devices_installed_by_technician_id_fkey"
    FOREIGN KEY ("installed_by_technician_id") REFERENCES "technicians"("id") ON DELETE SET NULL ON UPDATE CASCADE;
```

- [ ] **Step 5: Gerar o client e validar o schema**

```bash
cd backend && npx prisma validate && npx prisma generate
```

Esperado: `The schema at prisma/schema.prisma is valid` e `Generated Prisma Client`.

- [ ] **Step 6: Aplicar a migration no banco local e conferir**

```bash
cd backend && npx prisma migrate deploy
```

Esperado: `Applying migration 20260721120000_add_technicians`.

- [ ] **Step 7: Commit**

```bash
git add backend/prisma/schema.prisma backend/prisma/migrations/20260721120000_add_technicians/
git commit -m "feat(tecnicos): model Technician + campos de reserva no estoque"
```

---

### Task 2: Backend — módulo technicians (CRUD + senha provisória)

**Files:**
- Create: `backend/src/modules/technicians/technicians.service.ts`
- Create: `backend/src/modules/technicians/technicians.controller.ts`
- Create: `backend/src/modules/technicians/technicians.module.ts`
- Create: `backend/src/modules/technicians/dto/create-technician.dto.ts`
- Create: `backend/src/modules/technicians/dto/update-technician.dto.ts`
- Create: `backend/src/modules/technicians/technicians.service.spec.ts`
- Modify: `backend/src/app.module.ts`

**Interfaces:**
- Consumes: model `Technician` (Task 1).
- Produces: `TechniciansService.findAll(tenantId, search?)`, `.create(tenantId, dto) => { technician, tempPassword }`, `.update(id, tenantId, dto)`, `.remove(id, tenantId)`, `.resetPassword(id, tenantId) => { tempPassword }`, `.assignments(id, tenantId)`; helpers exportados `normalizeCpf(cpf: string): string` e `generateTempPassword(): string`.

- [ ] **Step 1: Escrever o teste que falha**

Criar `backend/src/modules/technicians/technicians.service.spec.ts`:

```typescript
import { normalizeCpf, generateTempPassword } from './technicians.service';

describe('normalizeCpf', () => {
  it('remove máscara e deixa só dígitos', () => {
    expect(normalizeCpf('137.915.777-35')).toBe('13791577735');
  });

  it('trata string vazia sem quebrar', () => {
    expect(normalizeCpf('')).toBe('');
  });
});

describe('generateTempPassword', () => {
  it('gera 8 caracteres', () => {
    expect(generateTempPassword()).toHaveLength(8);
  });

  it('nunca usa caracteres ambíguos (0, O, 1, I, L)', () => {
    for (let i = 0; i < 200; i++) {
      expect(generateTempPassword()).not.toMatch(/[0O1IL]/);
    }
  });

  it('não repete a mesma senha em sequência', () => {
    const senhas = new Set(Array.from({ length: 50 }, () => generateTempPassword()));
    expect(senhas.size).toBeGreaterThan(45);
  });
});
```

- [ ] **Step 2: Rodar o teste e ver falhar**

```bash
cd backend && npx jest src/modules/technicians/technicians.service.spec.ts
```

Esperado: FAIL — `Cannot find module './technicians.service'`.

- [ ] **Step 3: Escrever o service**

Criar `backend/src/modules/technicians/technicians.service.ts`:

```typescript
import {
  ConflictException,
  Injectable,
  Logger,
  NotFoundException,
} from '@nestjs/common';
import * as bcrypt from 'bcrypt';
import { PrismaService } from '../prisma/prisma.service';
import { CreateTechnicianDto } from './dto/create-technician.dto';
import { UpdateTechnicianDto } from './dto/update-technician.dto';

const BCRYPT_ROUNDS = 10;
// Alfabeto sem caracteres ambíguos — a senha vai ser lida em voz alta / digitada no celular.
const PASSWORD_ALPHABET = '23456789ABCDEFGHJKMNPQRSTUVWXYZ';

/** Remove máscara do CPF, deixando só dígitos. */
export function normalizeCpf(cpf: string): string {
  return (cpf || '').replace(/\D/g, '');
}

/** Senha provisória de 8 caracteres, sem 0/O/1/I/L. */
export function generateTempPassword(): string {
  let out = '';
  for (let i = 0; i < 8; i++) {
    out += PASSWORD_ALPHABET[Math.floor(Math.random() * PASSWORD_ALPHABET.length)];
  }
  return out;
}

@Injectable()
export class TechniciansService {
  private readonly logger = new Logger(TechniciansService.name);

  constructor(private prisma: PrismaService) {}

  async findAll(tenantId: string, search?: string) {
    const where: Record<string, unknown> = { tenantId, deletedAt: null };
    if (search) {
      const digits = normalizeCpf(search);
      where.OR = [
        { name: { contains: search, mode: 'insensitive' } },
        ...(digits ? [{ cpf: { contains: digits } }] : []),
      ];
    }

    const technicians = await this.prisma.technician.findMany({
      where,
      orderBy: { name: 'asc' },
      select: {
        id: true,
        name: true,
        cpf: true,
        phone: true,
        email: true,
        canReceiveEquipment: true,
        active: true,
        lastLoginAt: true,
        mustChangePassword: true,
        createdAt: true,
        _count: {
          select: {
            stockItems: { where: { associatedAt: null, deletedAt: null } },
            devices: true,
          },
        },
      },
    });

    return technicians.map(({ _count, ...t }) => ({
      ...t,
      assignedCount: _count.stockItems,
      installCount: _count.devices,
    }));
  }

  async create(tenantId: string, dto: CreateTechnicianDto) {
    const cpf = normalizeCpf(dto.cpf);
    const existing = await this.prisma.technician.findFirst({
      where: { tenantId, cpf, deletedAt: null },
    });
    if (existing) {
      throw new ConflictException(`Já existe um técnico com o CPF ${cpf}.`);
    }

    const tempPassword = generateTempPassword();
    const technician = await this.prisma.technician.create({
      data: {
        tenantId,
        name: dto.name.trim(),
        cpf,
        phone: dto.phone?.replace(/\D/g, '') || null,
        email: dto.email?.trim() || null,
        canReceiveEquipment: dto.canReceiveEquipment ?? true,
        password: await bcrypt.hash(tempPassword, BCRYPT_ROUNDS),
        mustChangePassword: true,
      },
      select: { id: true, name: true, cpf: true, phone: true, email: true },
    });

    this.logger.log(`Técnico criado: ${technician.name} (${cpf}) tenant=${tenantId}`);
    // tempPassword volta só nesta resposta — nunca é persistida em claro nem logada.
    return { technician, tempPassword };
  }

  async update(id: string, tenantId: string, dto: UpdateTechnicianDto) {
    await this.findOneOrFail(id, tenantId);
    return this.prisma.technician.update({
      where: { id },
      data: {
        ...(dto.name !== undefined && { name: dto.name.trim() }),
        ...(dto.phone !== undefined && { phone: dto.phone?.replace(/\D/g, '') || null }),
        ...(dto.email !== undefined && { email: dto.email?.trim() || null }),
        ...(dto.canReceiveEquipment !== undefined && {
          canReceiveEquipment: dto.canReceiveEquipment,
        }),
        ...(dto.active !== undefined && { active: dto.active }),
      },
      select: {
        id: true,
        name: true,
        cpf: true,
        phone: true,
        email: true,
        canReceiveEquipment: true,
        active: true,
      },
    });
  }

  async remove(id: string, tenantId: string) {
    await this.findOneOrFail(id, tenantId);
    const emCampo = await this.prisma.stockItem.count({
      where: { tenantId, assignedTechnicianId: id, associatedAt: null, deletedAt: null },
    });
    if (emCampo > 0) {
      throw new ConflictException(
        `Técnico tem ${emCampo} equipamento(s) reservado(s). Cancele as reservas antes de excluir.`,
      );
    }
    await this.prisma.technician.update({
      where: { id },
      data: { deletedAt: new Date(), active: false },
    });
    return { id };
  }

  async resetPassword(id: string, tenantId: string) {
    const technician = await this.findOneOrFail(id, tenantId);
    const tempPassword = generateTempPassword();
    await this.prisma.technician.update({
      where: { id },
      data: {
        password: await bcrypt.hash(tempPassword, BCRYPT_ROUNDS),
        mustChangePassword: true,
      },
    });
    this.logger.log(`Senha resetada pro técnico ${technician.name} tenant=${tenantId}`);
    return { technician: { id: technician.id, name: technician.name, cpf: technician.cpf }, tempPassword };
  }

  async assignments(id: string, tenantId: string) {
    await this.findOneOrFail(id, tenantId);
    return this.prisma.stockItem.findMany({
      where: {
        tenantId,
        assignedTechnicianId: id,
        associatedAt: null,
        deletedAt: null,
      },
      orderBy: { assignedAt: 'desc' },
      select: {
        id: true,
        imei: true,
        iccid: true,
        line: true,
        operator: true,
        server: true,
        assignedAt: true,
      },
    });
  }

  private async findOneOrFail(id: string, tenantId: string) {
    const technician = await this.prisma.technician.findFirst({
      where: { id, tenantId, deletedAt: null },
      select: { id: true, name: true, cpf: true },
    });
    if (!technician) throw new NotFoundException('Técnico não encontrado');
    return technician;
  }
}
```

- [ ] **Step 4: Rodar o teste e ver passar**

```bash
cd backend && npx jest src/modules/technicians/technicians.service.spec.ts
```

Esperado: PASS, 5 testes.

- [ ] **Step 5: Escrever os DTOs**

`backend/src/modules/technicians/dto/create-technician.dto.ts`:

```typescript
import { ApiProperty, ApiPropertyOptional } from '@nestjs/swagger';
import {
  IsBoolean,
  IsEmail,
  IsOptional,
  IsString,
  Length,
  Matches,
  MinLength,
} from 'class-validator';

export class CreateTechnicianDto {
  @ApiProperty({ example: 'Iury Queiroz de Oliveira' })
  @IsString()
  @MinLength(3, { message: 'Nome precisa ter ao menos 3 caracteres' })
  name: string;

  @ApiProperty({ example: '137.915.777-35' })
  @IsString()
  @Matches(/^\D*(\d\D*){11}$/, { message: 'CPF precisa ter 11 dígitos' })
  cpf: string;

  @ApiPropertyOptional({ example: '21999998888' })
  @IsOptional()
  @IsString()
  @Length(10, 20, { message: 'Celular inválido' })
  phone?: string;

  @ApiPropertyOptional({ example: 'tecnico@21go.com.br' })
  @IsOptional()
  @IsEmail({}, { message: 'E-mail inválido' })
  email?: string;

  @ApiPropertyOptional({ default: true })
  @IsOptional()
  @IsBoolean()
  canReceiveEquipment?: boolean;
}
```

`backend/src/modules/technicians/dto/update-technician.dto.ts`:

```typescript
import { ApiPropertyOptional } from '@nestjs/swagger';
import { IsBoolean, IsEmail, IsOptional, IsString, Length, MinLength } from 'class-validator';

export class UpdateTechnicianDto {
  @ApiPropertyOptional()
  @IsOptional()
  @IsString()
  @MinLength(3)
  name?: string;

  @ApiPropertyOptional()
  @IsOptional()
  @IsString()
  @Length(10, 20)
  phone?: string;

  @ApiPropertyOptional()
  @IsOptional()
  @IsEmail()
  email?: string;

  @ApiPropertyOptional()
  @IsOptional()
  @IsBoolean()
  canReceiveEquipment?: boolean;

  @ApiPropertyOptional()
  @IsOptional()
  @IsBoolean()
  active?: boolean;
}
```

- [ ] **Step 6: Escrever o controller**

`backend/src/modules/technicians/technicians.controller.ts`:

```typescript
import {
  Body,
  Controller,
  Delete,
  Get,
  Param,
  ParseUUIDPipe,
  Patch,
  Post,
  Query,
  Req,
  UseGuards,
} from '@nestjs/common';
import { ApiBearerAuth, ApiOperation, ApiTags } from '@nestjs/swagger';
import { Role } from '.prisma/client';
import { Roles } from '../../common/decorators';
import { RolesGuard } from '../../common/guards/roles.guard';
import { TechniciansService } from './technicians.service';
import { CreateTechnicianDto } from './dto/create-technician.dto';
import { UpdateTechnicianDto } from './dto/update-technician.dto';

interface AuthenticatedRequest {
  tenantId: string;
}

@ApiTags('Técnicos')
@ApiBearerAuth()
@Controller('technicians')
export class TechniciansController {
  constructor(private service: TechniciansService) {}

  @Get()
  @ApiOperation({ summary: 'Lista técnicos com contagem de reservas e instalações' })
  findAll(@Query('search') search: string | undefined, @Req() req: AuthenticatedRequest) {
    return this.service.findAll(req.tenantId, search);
  }

  @Get(':id/assignments')
  @ApiOperation({ summary: 'Equipamentos reservados pro técnico' })
  assignments(@Param('id', ParseUUIDPipe) id: string, @Req() req: AuthenticatedRequest) {
    return this.service.assignments(id, req.tenantId);
  }

  @Post()
  @UseGuards(RolesGuard)
  @Roles(Role.SUPER_ADMIN, Role.ADMIN, Role.OPERATOR)
  @ApiOperation({ summary: 'Cadastra técnico e devolve a senha provisória (uma única vez)' })
  create(@Body() dto: CreateTechnicianDto, @Req() req: AuthenticatedRequest) {
    return this.service.create(req.tenantId, dto);
  }

  @Post(':id/reset-password')
  @UseGuards(RolesGuard)
  @Roles(Role.SUPER_ADMIN, Role.ADMIN, Role.OPERATOR)
  @ApiOperation({ summary: 'Gera nova senha provisória' })
  resetPassword(@Param('id', ParseUUIDPipe) id: string, @Req() req: AuthenticatedRequest) {
    return this.service.resetPassword(id, req.tenantId);
  }

  @Patch(':id')
  @UseGuards(RolesGuard)
  @Roles(Role.SUPER_ADMIN, Role.ADMIN, Role.OPERATOR)
  @ApiOperation({ summary: 'Atualiza dados do técnico' })
  update(
    @Param('id', ParseUUIDPipe) id: string,
    @Body() dto: UpdateTechnicianDto,
    @Req() req: AuthenticatedRequest,
  ) {
    return this.service.update(id, req.tenantId, dto);
  }

  @Delete(':id')
  @UseGuards(RolesGuard)
  @Roles(Role.SUPER_ADMIN, Role.ADMIN)
  @ApiOperation({ summary: 'Remove técnico (soft delete)' })
  remove(@Param('id', ParseUUIDPipe) id: string, @Req() req: AuthenticatedRequest) {
    return this.service.remove(id, req.tenantId);
  }
}
```

- [ ] **Step 7: Registrar o módulo**

`backend/src/modules/technicians/technicians.module.ts`:

```typescript
import { Module } from '@nestjs/common';
import { TechniciansService } from './technicians.service';
import { TechniciansController } from './technicians.controller';

@Module({
  controllers: [TechniciansController],
  providers: [TechniciansService],
  exports: [TechniciansService],
})
export class TechniciansModule {}
```

Em `backend/src/app.module.ts`, importar `TechniciansModule` e adicionar ao array `imports` junto dos outros módulos de domínio.

- [ ] **Step 8: Compilar**

```bash
cd backend && npm run build
```

Esperado: build sem erro de tipo.

- [ ] **Step 9: Commit**

```bash
git add backend/src/modules/technicians backend/src/app.module.ts
git commit -m "feat(tecnicos): CRUD de tecnicos com senha provisoria"
```

---

### Task 3: Backend — reserva em lote no estoque

**Files:**
- Modify: `backend/src/modules/stock/stock.service.ts`
- Modify: `backend/src/modules/stock/stock.controller.ts`
- Modify: `backend/src/modules/stock/dto/filter-stock.dto.ts`
- Create: `backend/src/modules/stock/dto/assign-stock.dto.ts`

**Interfaces:**
- Consumes: `Technician` (Task 1).
- Produces: `StockService.assign(tenantId, dto, userId) => { ok: number; skipped: Array<{ imei: string; motivo: string }> }`, `.unassign(tenantId, stockItemIds)`; `FilterStockDto.assignment?: 'free' | 'assigned'`.

- [ ] **Step 1: Criar o DTO de lote**

`backend/src/modules/stock/dto/assign-stock.dto.ts`:

```typescript
import { ApiProperty, ApiPropertyOptional } from '@nestjs/swagger';
import { ArrayMaxSize, ArrayNotEmpty, IsArray, IsOptional, IsUUID } from 'class-validator';

export class AssignStockDto {
  @ApiProperty({ type: [String], description: 'IDs dos itens de estoque' })
  @IsArray()
  @ArrayNotEmpty({ message: 'Selecione ao menos um equipamento' })
  @ArrayMaxSize(500, { message: 'Máximo de 500 equipamentos por vez' })
  @IsUUID('4', { each: true })
  stockItemIds: string[];

  @ApiPropertyOptional({ description: 'Técnico que recebe. Omitir = cancelar reserva.' })
  @IsOptional()
  @IsUUID()
  technicianId?: string;
}
```

- [ ] **Step 2: Adicionar o filtro de reserva ao FilterStockDto**

Em `backend/src/modules/stock/dto/filter-stock.dto.ts`, adicionar:

```typescript
  @ApiPropertyOptional({ enum: ['free', 'assigned'], description: 'free = sem técnico; assigned = com técnico' })
  @IsOptional()
  @IsIn(['free', 'assigned'])
  assignment?: 'free' | 'assigned';
```

Importar `IsIn` de `class-validator` e `ApiPropertyOptional` de `@nestjs/swagger` se ainda não estiverem importados no arquivo.

- [ ] **Step 3: Trazer o técnico na listagem e aplicar o filtro**

Em `backend/src/modules/stock/stock.service.ts`, no `findAll`, após o bloco de `search` (linha ~90), adicionar:

```typescript
    if (filters.assignment === 'free') where.assignedTechnicianId = null;
    if (filters.assignment === 'assigned') where.assignedTechnicianId = { not: null };
```

E no `findMany`, adicionar o `include` do técnico:

```typescript
      this.prisma.stockItem.findMany({
        where,
        skip: (page - 1) * perPage,
        take: perPage,
        orderBy: { createdAt: 'desc' },
        include: {
          assignedTechnician: { select: { id: true, name: true } },
        },
      }),
```

Também desestruturar `assignment` junto dos outros filtros na primeira linha do método:

```typescript
    const { page, perPage, search, status, operator, assignment } = filters;
```

- [ ] **Step 4: Implementar assign/unassign no service**

Ainda em `stock.service.ts`, adicionar antes do `importFromBuffer`:

```typescript
  /**
   * Reserva equipamentos pro login do técnico, em lote. Um item já reservado por
   * outro técnico não aborta o lote — volta em `skipped` com o motivo, pra tela
   * mostrar exatamente o que não foi.
   */
  async assign(
    tenantId: string,
    dto: { stockItemIds: string[]; technicianId?: string },
    userId: string,
  ) {
    const { stockItemIds, technicianId } = dto;
    if (!technicianId) {
      throw new BadRequestException('Informe o técnico que vai receber.');
    }

    const technician = await this.prisma.technician.findFirst({
      where: { id: technicianId, tenantId, deletedAt: null },
      select: { id: true, name: true, active: true, canReceiveEquipment: true },
    });
    if (!technician) throw new NotFoundException('Técnico não encontrado');
    if (!technician.active) {
      throw new UnprocessableEntityException(`${technician.name} está inativo.`);
    }
    if (!technician.canReceiveEquipment) {
      throw new UnprocessableEntityException(
        `${technician.name} não está habilitado a receber equipamentos.`,
      );
    }

    const items = await this.prisma.stockItem.findMany({
      where: { id: { in: stockItemIds }, tenantId, deletedAt: null },
      select: {
        id: true,
        imei: true,
        associatedAt: true,
        assignedTechnicianId: true,
        assignedTechnician: { select: { name: true } },
      },
    });

    const skipped: Array<{ imei: string; motivo: string }> = [];
    const okIds: string[] = [];

    for (const item of items) {
      if (item.associatedAt) {
        skipped.push({ imei: item.imei, motivo: 'já instalado' });
      } else if (item.assignedTechnicianId && item.assignedTechnicianId !== technicianId) {
        skipped.push({
          imei: item.imei,
          motivo: `já está com ${item.assignedTechnician?.name ?? 'outro técnico'}`,
        });
      } else {
        okIds.push(item.id);
      }
    }

    const encontrados = new Set(items.map((i) => i.id));
    for (const id of stockItemIds) {
      if (!encontrados.has(id)) {
        skipped.push({ imei: id, motivo: 'não encontrado no estoque' });
      }
    }

    if (okIds.length > 0) {
      await this.prisma.stockItem.updateMany({
        where: { id: { in: okIds }, tenantId },
        data: {
          assignedTechnicianId: technicianId,
          assignedAt: new Date(),
          assignedById: userId,
        },
      });
    }

    this.logger.log(
      `Reserva: ${okIds.length} equipamento(s) pro técnico ${technician.name} (${skipped.length} ignorados)`,
    );
    return { ok: okIds.length, skipped };
  }

  /** Devolve equipamentos ao estoque livre (cancela a reserva). */
  async unassign(tenantId: string, stockItemIds: string[]) {
    const result = await this.prisma.stockItem.updateMany({
      where: {
        id: { in: stockItemIds },
        tenantId,
        deletedAt: null,
        associatedAt: null,
      },
      data: { assignedTechnicianId: null, assignedAt: null, assignedById: null },
    });
    return { ok: result.count, skipped: [] as Array<{ imei: string; motivo: string }> };
  }
```

- [ ] **Step 5: Expor as rotas no controller**

Em `backend/src/modules/stock/stock.controller.ts`, adicionar o import do DTO e as duas rotas (antes de `@Delete(':id')`). Note que a interface local precisa passar a expor o usuário:

```typescript
interface AuthenticatedRequest {
  tenantId: string;
  user: { id: string };
}
```

```typescript
  @Post('assign')
  @UseGuards(RolesGuard)
  @Roles(Role.SUPER_ADMIN, Role.ADMIN, Role.OPERATOR)
  @ApiOperation({ summary: 'Reserva equipamentos pro login do técnico (lote)' })
  assign(@Body() dto: AssignStockDto, @Req() req: AuthenticatedRequest) {
    return this.stockService.assign(req.tenantId, dto, req.user.id);
  }

  @Post('unassign')
  @UseGuards(RolesGuard)
  @Roles(Role.SUPER_ADMIN, Role.ADMIN, Role.OPERATOR)
  @ApiOperation({ summary: 'Cancela a reserva e devolve ao estoque livre' })
  unassign(@Body() dto: AssignStockDto, @Req() req: AuthenticatedRequest) {
    return this.stockService.unassign(req.tenantId, dto.stockItemIds);
  }
```

- [ ] **Step 6: Compilar e testar por curl no backend local**

```bash
cd backend && npm run build && npm run start:dev
```

Em outro terminal (substituir `$TOKEN` por um JWT de ADMIN obtido em `/api/v1/auth/login`, e os UUIDs por valores reais do banco):

```bash
curl -s -X POST http://localhost:3001/api/v1/stock/assign \
  -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" \
  -d '{"stockItemIds":["<id1>","<id2>"],"technicianId":"<tecnicoId>"}'
```

Esperado: `{"ok":2,"skipped":[]}`. Repetir o mesmo comando com outro `technicianId`: esperado `{"ok":0,"skipped":[{"imei":"...","motivo":"já está com ..."}]}`.

- [ ] **Step 7: Commit**

```bash
git add backend/src/modules/stock
git commit -m "feat(estoque): reserva de equipamentos por tecnico em lote"
```

---

### Task 4: Backend — auth do técnico

**Files:**
- Create: `backend/src/modules/tech/guards/technician-jwt.guard.ts`
- Create: `backend/src/modules/tech/decorators/current-technician.decorator.ts`
- Create: `backend/src/modules/tech/tech-auth.service.ts`
- Create: `backend/src/modules/tech/tech-auth.controller.ts`
- Create: `backend/src/modules/tech/dto/tech-login.dto.ts`
- Create: `backend/src/modules/tech/dto/change-password.dto.ts`
- Create: `backend/src/modules/tech/tech.module.ts`
- Modify: `backend/src/app.module.ts`

**Interfaces:**
- Consumes: `Technician` (Task 1), `normalizeCpf` (Task 2).
- Produces: `TechnicianJwtGuard` populando `req.technician = { id, name, tenantId, mustChangePassword }` e `req.tenantId`; decorator `@CurrentTechnician()`; payload JWT `{ sub, type: 'technician', tenantId, name }`.

- [ ] **Step 1: Escrever o guard**

`backend/src/modules/tech/guards/technician-jwt.guard.ts`:

```typescript
import {
  CanActivate,
  ExecutionContext,
  Injectable,
  UnauthorizedException,
} from '@nestjs/common';
import { ConfigService } from '@nestjs/config';
import { JwtService } from '@nestjs/jwt';
import { PrismaService } from '../../prisma/prisma.service';

export interface TechnicianPayload {
  sub: string;
  type: 'technician';
  tenantId: string;
  name: string;
}

/**
 * Guard do PWA do técnico. Valida JWT com `type: 'technician'` e popula
 * `req.technician` + `req.tenantId`. As rotas são `@Public()` pra pular o
 * JwtAuthGuard global (que busca na tabela User) — este guard assume.
 * Recarrega o técnico do banco a cada request: desativar o técnico no painel
 * derruba o acesso na hora, sem esperar o token expirar.
 */
@Injectable()
export class TechnicianJwtGuard implements CanActivate {
  constructor(
    private readonly jwt: JwtService,
    private readonly config: ConfigService,
    private readonly prisma: PrismaService,
  ) {}

  async canActivate(context: ExecutionContext): Promise<boolean> {
    const req = context.switchToHttp().getRequest();
    const header: string | undefined = req.headers?.authorization;

    if (!header?.startsWith('Bearer ')) {
      throw new UnauthorizedException('Token ausente');
    }

    let payload: TechnicianPayload;
    try {
      payload = this.jwt.verify<TechnicianPayload>(header.slice(7), {
        secret: this.config.get<string>('jwt.secret')!,
      });
    } catch {
      throw new UnauthorizedException('Token inválido ou expirado');
    }

    if (payload.type !== 'technician') {
      throw new UnauthorizedException('Token não é de técnico');
    }

    const technician = await this.prisma.technician.findFirst({
      where: { id: payload.sub, deletedAt: null, active: true },
      select: { id: true, name: true, tenantId: true, mustChangePassword: true },
    });
    if (!technician) throw new UnauthorizedException('Técnico inativo ou removido');

    req.technician = technician;
    req.tenantId = technician.tenantId;
    return true;
  }
}
```

- [ ] **Step 2: Escrever o decorator**

`backend/src/modules/tech/decorators/current-technician.decorator.ts`:

```typescript
import { createParamDecorator, ExecutionContext } from '@nestjs/common';

export interface TechnicianContext {
  id: string;
  name: string;
  tenantId: string;
  mustChangePassword: boolean;
}

export const CurrentTechnician = createParamDecorator(
  (data: keyof TechnicianContext | undefined, ctx: ExecutionContext) => {
    const req = ctx.switchToHttp().getRequest();
    const technician: TechnicianContext = req.technician;
    return data ? technician?.[data] : technician;
  },
);
```

- [ ] **Step 3: Escrever os DTOs**

`backend/src/modules/tech/dto/tech-login.dto.ts`:

```typescript
import { ApiProperty } from '@nestjs/swagger';
import { IsString, Matches, MinLength } from 'class-validator';

export class TechLoginDto {
  @ApiProperty({ example: '137.915.777-35' })
  @IsString()
  @Matches(/^\D*(\d\D*){11}$/, { message: 'CPF precisa ter 11 dígitos' })
  cpf: string;

  @ApiProperty()
  @IsString()
  @MinLength(6, { message: 'Senha precisa ter ao menos 6 caracteres' })
  password: string;
}
```

`backend/src/modules/tech/dto/change-password.dto.ts`:

```typescript
import { ApiProperty } from '@nestjs/swagger';
import { IsString, MinLength } from 'class-validator';

export class ChangePasswordDto {
  @ApiProperty()
  @IsString()
  currentPassword: string;

  @ApiProperty()
  @IsString()
  @MinLength(6, { message: 'A nova senha precisa ter ao menos 6 caracteres' })
  newPassword: string;
}
```

- [ ] **Step 4: Escrever o service de auth**

`backend/src/modules/tech/tech-auth.service.ts`:

```typescript
import { Injectable, Logger, UnauthorizedException } from '@nestjs/common';
import { JwtService } from '@nestjs/jwt';
import * as bcrypt from 'bcrypt';
import { PrismaService } from '../prisma/prisma.service';
import { normalizeCpf } from '../technicians/technicians.service';
import { TechLoginDto } from './dto/tech-login.dto';
import { ChangePasswordDto } from './dto/change-password.dto';

const BCRYPT_ROUNDS = 10;

@Injectable()
export class TechAuthService {
  private readonly logger = new Logger(TechAuthService.name);

  constructor(
    private readonly prisma: PrismaService,
    private readonly jwt: JwtService,
  ) {}

  async login(dto: TechLoginDto) {
    const cpf = normalizeCpf(dto.cpf);

    // Mesmo CPF pode existir em tenants diferentes — valida o hash 1 a 1.
    const candidates = await this.prisma.technician.findMany({
      where: { cpf, deletedAt: null, active: true, password: { not: null } },
      select: {
        id: true,
        name: true,
        cpf: true,
        tenantId: true,
        password: true,
        mustChangePassword: true,
      },
    });

    for (const t of candidates) {
      if (t.password && (await bcrypt.compare(dto.password, t.password))) {
        await this.prisma.technician.update({
          where: { id: t.id },
          data: { lastLoginAt: new Date() },
        });
        const { password: _omit, ...technician } = t;
        return {
          accessToken: this.jwt.sign({
            sub: t.id,
            type: 'technician' as const,
            tenantId: t.tenantId,
            name: t.name,
          }),
          technician,
        };
      }
    }

    // Mensagem genérica — não revela se o CPF existe nem se está inativo.
    throw new UnauthorizedException('CPF ou senha inválidos');
  }

  async me(technicianId: string) {
    const technician = await this.prisma.technician.findFirst({
      where: { id: technicianId, deletedAt: null },
      select: {
        id: true,
        name: true,
        cpf: true,
        phone: true,
        mustChangePassword: true,
        tenant: { select: { id: true, name: true, logoUrl: true, primaryColor: true } },
      },
    });
    if (!technician) throw new UnauthorizedException('Técnico não encontrado');
    return technician;
  }

  async changePassword(technicianId: string, dto: ChangePasswordDto) {
    const technician = await this.prisma.technician.findFirst({
      where: { id: technicianId, deletedAt: null, active: true },
      select: { id: true, name: true, password: true },
    });
    if (!technician?.password) throw new UnauthorizedException('Técnico não encontrado');

    const ok = await bcrypt.compare(dto.currentPassword, technician.password);
    if (!ok) throw new UnauthorizedException('Senha atual incorreta');

    await this.prisma.technician.update({
      where: { id: technician.id },
      data: {
        password: await bcrypt.hash(dto.newPassword, BCRYPT_ROUNDS),
        mustChangePassword: false,
      },
    });
    this.logger.log(`Técnico ${technician.name} trocou a senha`);
    return { ok: true };
  }
}
```

- [ ] **Step 5: Escrever o controller de auth**

`backend/src/modules/tech/tech-auth.controller.ts`:

```typescript
import { Body, Controller, Get, Post, UseGuards } from '@nestjs/common';
import { ApiBearerAuth, ApiOperation, ApiTags } from '@nestjs/swagger';
import { Public } from '../../common/decorators';
import { TechAuthService } from './tech-auth.service';
import { TechLoginDto } from './dto/tech-login.dto';
import { ChangePasswordDto } from './dto/change-password.dto';
import { TechnicianJwtGuard } from './guards/technician-jwt.guard';
import { CurrentTechnician } from './decorators/current-technician.decorator';

@ApiTags('Técnico - Auth')
@Controller('tech/auth')
export class TechAuthController {
  constructor(private readonly service: TechAuthService) {}

  @Public()
  @Post('login')
  @ApiOperation({ summary: 'Login do técnico por CPF + senha' })
  login(@Body() dto: TechLoginDto) {
    return this.service.login(dto);
  }

  @Public()
  @UseGuards(TechnicianJwtGuard)
  @ApiBearerAuth()
  @Get('me')
  @ApiOperation({ summary: 'Dados do técnico logado' })
  me(@CurrentTechnician('id') id: string) {
    return this.service.me(id);
  }

  @Public()
  @UseGuards(TechnicianJwtGuard)
  @ApiBearerAuth()
  @Post('change-password')
  @ApiOperation({ summary: 'Troca de senha (obrigatória no primeiro acesso)' })
  changePassword(@CurrentTechnician('id') id: string, @Body() dto: ChangePasswordDto) {
    return this.service.changePassword(id, dto);
  }
}
```

- [ ] **Step 6: Criar o módulo (com o service de campo da Task 5 já previsto)**

`backend/src/modules/tech/tech.module.ts`:

```typescript
import { Module } from '@nestjs/common';
import { JwtModule } from '@nestjs/jwt';
import { ConfigService } from '@nestjs/config';
import { StockModule } from '../stock/stock.module';
import { TraccarModule } from '../traccar/traccar.module';
import { HinovaModule } from '../hinova/hinova.module';
import { TechAuthService } from './tech-auth.service';
import { TechAuthController } from './tech-auth.controller';
import { TechnicianJwtGuard } from './guards/technician-jwt.guard';

/**
 * PWA do técnico instalador (/tecnico). Auth por CPF + senha, rotas sob /tech/*
 * que só enxergam os equipamentos reservados pro próprio técnico.
 */
@Module({
  imports: [
    StockModule,
    TraccarModule,
    HinovaModule,
    JwtModule.registerAsync({
      inject: [ConfigService],
      useFactory: (config: ConfigService) => ({
        secret: config.get<string>('jwt.secret')!,
        signOptions: { expiresIn: config.get<string>('jwt.expiration')! as any },
      }),
    }),
  ],
  controllers: [TechAuthController],
  providers: [TechAuthService, TechnicianJwtGuard],
})
export class TechModule {}
```

Registrar `TechModule` no array `imports` de `backend/src/app.module.ts`. Conferir que `StockModule` exporta `StockService` (adicionar `exports: [StockService]` se faltar) e que `HinovaModule` exporta o provider `HINOVA_CLIENT`.

- [ ] **Step 7: Verificar login por curl**

Com o backend rodando e um técnico criado pela rota da Task 2:

```bash
curl -s -X POST http://localhost:3001/api/v1/tech/auth/login \
  -H "Content-Type: application/json" \
  -d '{"cpf":"13791577735","password":"<tempPassword>"}'
```

Esperado: JSON com `accessToken` e `technician.mustChangePassword: true`.

Verificar o isolamento — o token do técnico não pode abrir rota do painel:

```bash
curl -s -o /dev/null -w "%{http_code}" http://localhost:3001/api/v1/vehicles \
  -H "Authorization: Bearer <accessToken do tecnico>"
```

Esperado: `401`.

- [ ] **Step 8: Commit**

```bash
git add backend/src/modules/tech backend/src/app.module.ts backend/src/modules/stock/stock.module.ts
git commit -m "feat(tecnicos): auth do tecnico por CPF com guard isolado"
```

---

### Task 5: Backend — rotas de campo do técnico

**Files:**
- Create: `backend/src/modules/tech/tech-field.service.ts`
- Create: `backend/src/modules/tech/tech-field.controller.ts`
- Create: `backend/src/modules/tech/dto/finish-install.dto.ts`
- Modify: `backend/src/modules/tech/tech.module.ts`
- Modify: `backend/src/modules/stock/stock.service.ts`
- Modify: `backend/src/modules/stock/dto/associate-stock.dto.ts`

**Interfaces:**
- Consumes: `TechnicianJwtGuard`, `@CurrentTechnician()` (Task 4); `StockService.associate` (existente).
- Produces: `StockService.associate(id, tenantId, dto)` passa a aceitar `dto.technicianId?: string`; `TechFieldService.assignments`, `.lookup`, `.signal`, `.finish`.

- [ ] **Step 1: Aceitar technicianId no DTO de associação**

Em `backend/src/modules/stock/dto/associate-stock.dto.ts`, adicionar:

```typescript
  @ApiPropertyOptional({ description: 'Técnico que executou (preenchido pelo PWA)' })
  @IsOptional()
  @IsUUID()
  technicianId?: string;
```

Importar `ApiPropertyOptional`, `IsOptional` e `IsUUID` se faltarem.

- [ ] **Step 2: Gravar o vínculo do técnico no Device**

Em `backend/src/modules/stock/stock.service.ts`, dentro de `associate()`, no bloco do Device (linha ~248), acrescentar o campo nos dois ramos:

```typescript
      const device = existingDevice
        ? await tx.device.update({
            where: { id: existingDevice.id },
            data: {
              vehicleId: vehicle.id,
              status: 'INSTALLED',
              installedAt: new Date(),
              installedBy: technicianName,
              installedByTechnicianId: dto.technicianId ?? null,
              installLocation,
            },
          })
        : await tx.device.create({
            data: {
              imei: item.imei,
              model: 'OTHER',
              status: 'INSTALLED',
              vehicleId: vehicle.id,
              tenantId,
              installedAt: new Date(),
              installedBy: technicianName,
              installedByTechnicianId: dto.technicianId ?? null,
              installLocation,
            },
          });
```

E no update final do item (linha ~273), limpar a reserva — instalado sai da lista do técnico:

```typescript
      await tx.stockItem.update({
        where: { id: item.id },
        data: {
          associatedAt: new Date(),
          deviceId: device.id,
          assignedTechnicianId: null,
          assignedAt: null,
        },
      });
```

- [ ] **Step 3: Criar o DTO de finalização**

`backend/src/modules/tech/dto/finish-install.dto.ts`:

```typescript
import { ApiProperty, ApiPropertyOptional } from '@nestjs/swagger';
import { IsOptional, IsString, Length, MinLength } from 'class-validator';

export class FinishInstallDto {
  @ApiProperty({ example: 'KQV7463' })
  @IsString()
  @Length(7, 8, { message: 'Placa inválida' })
  placa: string;

  @ApiProperty({ example: 'Painel — atrás do console central' })
  @IsString()
  @MinLength(3, { message: 'Informe o local de instalação' })
  installLocation: string;

  @ApiPropertyOptional()
  @IsOptional()
  @IsString()
  notes?: string;
}
```

- [ ] **Step 4: Escrever o service de campo**

`backend/src/modules/tech/tech-field.service.ts`:

```typescript
import { Inject, Injectable, NotFoundException } from '@nestjs/common';
import { PrismaService } from '../prisma/prisma.service';
import { StockService } from '../stock/stock.service';
import { TraccarService } from '../traccar/traccar.service';
import { HINOVA_CLIENT, type IHinovaClient } from '../hinova/hinova.interface';
import { FinishInstallDto } from './dto/finish-install.dto';

@Injectable()
export class TechFieldService {
  constructor(
    private readonly prisma: PrismaService,
    private readonly stock: StockService,
    private readonly traccar: TraccarService,
    @Inject(HINOVA_CLIENT) private readonly hinova: IHinovaClient,
  ) {}

  /** Equipamentos reservados pro técnico logado. Nunca vaza item de outro. */
  async assignments(technicianId: string, tenantId: string) {
    return this.prisma.stockItem.findMany({
      where: {
        tenantId,
        assignedTechnicianId: technicianId,
        associatedAt: null,
        deletedAt: null,
      },
      orderBy: { assignedAt: 'desc' },
      select: {
        id: true,
        imei: true,
        iccid: true,
        line: true,
        operator: true,
        server: true,
        status: true,
        assignedAt: true,
      },
    });
  }

  /** Consulta a placa no SGA antes de finalizar. */
  async lookup(placa: string) {
    return this.hinova.lookupByPlate(placa);
  }

  /** O rastreador já reportou posição? Confere no Traccar pelo IMEI. */
  async signal(stockItemId: string, technicianId: string, tenantId: string) {
    const item = await this.findAssignedOrFail(stockItemId, technicianId, tenantId);
    try {
      const device = await this.traccar.getDeviceByUniqueId(item.imei);
      if (!device) return { online: false, lastUpdate: null, motivo: 'não encontrado no servidor GPS' };
      return {
        online: device.status === 'online',
        lastUpdate: device.lastUpdate ?? null,
        motivo: null,
      };
    } catch {
      return { online: false, lastUpdate: null, motivo: 'servidor GPS indisponível' };
    }
  }

  /** Finaliza a instalação reusando exatamente o motor do painel. */
  async finish(
    stockItemId: string,
    technicianId: string,
    technicianName: string,
    tenantId: string,
    dto: FinishInstallDto,
  ) {
    await this.findAssignedOrFail(stockItemId, technicianId, tenantId);
    return this.stock.associate(stockItemId, tenantId, {
      placa: dto.placa,
      technicianName,
      technicianId,
      installLocation: dto.installLocation,
      notes: dto.notes,
    });
  }

  /**
   * 404 (não 403) quando o item existe mas é de outro técnico — não confirma
   * a existência do IMEI pra quem não tem direito a ele.
   */
  private async findAssignedOrFail(id: string, technicianId: string, tenantId: string) {
    const item = await this.prisma.stockItem.findFirst({
      where: {
        id,
        tenantId,
        assignedTechnicianId: technicianId,
        associatedAt: null,
        deletedAt: null,
      },
      select: { id: true, imei: true },
    });
    if (!item) throw new NotFoundException('Equipamento não está na sua lista');
    return item;
  }
}
```

Se `AssociateStockDto` não tiver o campo `notes`, remover `notes: dto.notes` da chamada acima.

- [ ] **Step 5: Escrever o controller de campo**

`backend/src/modules/tech/tech-field.controller.ts`:

```typescript
import { Body, Controller, Get, Param, ParseUUIDPipe, Post, Query, UseGuards } from '@nestjs/common';
import { ApiBearerAuth, ApiOperation, ApiTags } from '@nestjs/swagger';
import { Public } from '../../common/decorators';
import { TechnicianJwtGuard } from './guards/technician-jwt.guard';
import { CurrentTechnician, type TechnicianContext } from './decorators/current-technician.decorator';
import { TechFieldService } from './tech-field.service';
import { FinishInstallDto } from './dto/finish-install.dto';

@ApiTags('Técnico - Campo')
@Public()
@UseGuards(TechnicianJwtGuard)
@ApiBearerAuth()
@Controller('tech')
export class TechFieldController {
  constructor(private readonly service: TechFieldService) {}

  @Get('assignments')
  @ApiOperation({ summary: 'Equipamentos reservados pro técnico logado' })
  assignments(@CurrentTechnician() tech: TechnicianContext) {
    return this.service.assignments(tech.id, tech.tenantId);
  }

  @Get('lookup')
  @ApiOperation({ summary: 'Consulta a placa no SGA' })
  lookup(@Query('placa') placa: string) {
    return this.service.lookup(placa);
  }

  @Get('assignments/:id/signal')
  @ApiOperation({ summary: 'O rastreador já reportou posição?' })
  signal(@Param('id', ParseUUIDPipe) id: string, @CurrentTechnician() tech: TechnicianContext) {
    return this.service.signal(id, tech.id, tech.tenantId);
  }

  @Post('assignments/:id/finish')
  @ApiOperation({ summary: 'Finaliza a instalação: cria cliente, veículo e rastreador' })
  finish(
    @Param('id', ParseUUIDPipe) id: string,
    @Body() dto: FinishInstallDto,
    @CurrentTechnician() tech: TechnicianContext,
  ) {
    return this.service.finish(id, tech.id, tech.name, tech.tenantId, dto);
  }
}
```

Adicionar `TechFieldController` em `controllers` e `TechFieldService` em `providers` no `tech.module.ts`.

- [ ] **Step 6: Verificar por curl (isolamento e fluxo)**

```bash
TECH=<accessToken do tecnico A>
curl -s http://localhost:3001/api/v1/tech/assignments -H "Authorization: Bearer $TECH"
```

Esperado: só os IMEIs reservados pro técnico A.

```bash
curl -s -o /dev/null -w "%{http_code}" -X POST \
  http://localhost:3001/api/v1/tech/assignments/<id-de-item-do-tecnico-B>/finish \
  -H "Authorization: Bearer $TECH" -H "Content-Type: application/json" \
  -d '{"placa":"ABC1D23","installLocation":"Painel"}'
```

Esperado: `404`.

- [ ] **Step 7: Commit**

```bash
git add backend/src/modules/tech backend/src/modules/stock
git commit -m "feat(tecnicos): rotas de campo - lista, consulta SGA, sinal e finalizar instalacao"
```

---

### Task 6: Frontend — página /tecnicos

**Files:**
- Create: `frontend/dashboard/src/types/technician.ts`
- Create: `frontend/dashboard/src/app/(dashboard)/tecnicos/page.tsx`
- Create: `frontend/dashboard/src/components/technicians/technician-form-dialog.tsx`
- Create: `frontend/dashboard/src/components/technicians/technician-password-dialog.tsx`
- Modify: `frontend/dashboard/src/lib/api.ts`
- Modify: `frontend/dashboard/src/components/layout/sidebar.tsx`

**Interfaces:**
- Consumes: rotas da Task 2.
- Produces: `techniciansApi` em `lib/api.ts`; tipos `Technician`, `TechnicianAssignment`, `CreateTechnicianPayload`.

- [ ] **Step 1: Criar os tipos**

`frontend/dashboard/src/types/technician.ts`:

```typescript
export interface Technician {
  id: string;
  name: string;
  cpf: string;
  phone: string | null;
  email: string | null;
  canReceiveEquipment: boolean;
  active: boolean;
  mustChangePassword: boolean;
  lastLoginAt: string | null;
  createdAt: string;
  assignedCount: number;
  installCount: number;
}

export interface TechnicianAssignment {
  id: string;
  imei: string;
  iccid: string | null;
  line: string | null;
  operator: string | null;
  server: string | null;
  assignedAt: string | null;
}

export interface CreateTechnicianPayload {
  name: string;
  cpf: string;
  phone?: string;
  email?: string;
  canReceiveEquipment?: boolean;
}

export interface TechnicianWithPassword {
  technician: { id: string; name: string; cpf: string; phone?: string | null };
  tempPassword: string;
}
```

- [ ] **Step 2: Adicionar o client de API**

Em `frontend/dashboard/src/lib/api.ts`, importar os tipos e adicionar ao final:

```typescript
export const techniciansApi = {
  getAll: async (search?: string): Promise<Technician[]> => {
    const res = await api.get<ApiResponse<Technician[]>>('/technicians', {
      params: search ? { search } : undefined,
    });
    return res.data.data;
  },
  create: async (payload: CreateTechnicianPayload): Promise<TechnicianWithPassword> => {
    const res = await api.post<ApiResponse<TechnicianWithPassword>>('/technicians', payload);
    return res.data.data;
  },
  update: async (id: string, payload: Partial<CreateTechnicianPayload> & { active?: boolean }): Promise<Technician> => {
    const res = await api.patch<ApiResponse<Technician>>(`/technicians/${id}`, payload);
    return res.data.data;
  },
  remove: async (id: string): Promise<void> => {
    await api.delete(`/technicians/${id}`);
  },
  resetPassword: async (id: string): Promise<TechnicianWithPassword> => {
    const res = await api.post<ApiResponse<TechnicianWithPassword>>(`/technicians/${id}/reset-password`, {});
    return res.data.data;
  },
  assignments: async (id: string): Promise<TechnicianAssignment[]> => {
    const res = await api.get<ApiResponse<TechnicianAssignment[]>>(`/technicians/${id}/assignments`);
    return res.data.data;
  },
};
```

Conferir no interceptor de resposta do arquivo se as demais chamadas usam `res.data.data` (envelope `ApiResponse`) — se o backend devolver o array cru nessas rotas, usar `res.data`. Validar com um `curl` da rota antes de escrever.

- [ ] **Step 3: Adicionar o item no sidebar**

Em `frontend/dashboard/src/components/layout/sidebar.tsx`, importar `HardHat` de `lucide-react` e adicionar ao `navItems`, logo depois de `/clientes`:

```typescript
  { href: '/tecnicos', label: 'Técnicos', icon: HardHat, roles: NON_CLIENT_ROLES },
```

- [ ] **Step 4: Escrever o dialog de senha provisória**

`frontend/dashboard/src/components/technicians/technician-password-dialog.tsx` — recebe `{ data: TechnicianWithPassword | null, open, onOpenChange }`, mostra a senha em fonte mono grande e dois botões: "Copiar senha" e "Copiar mensagem do WhatsApp". A mensagem é montada assim:

```typescript
const mensagem = [
  `Olá ${data.technician.name}! Seu acesso ao 21 GO:`,
  `${window.location.origin}/tecnico`,
  `CPF: ${data.technician.cpf}`,
  `Senha: ${data.tempPassword}`,
  'Troque a senha no primeiro acesso.',
].join('\n');
await navigator.clipboard.writeText(mensagem);
toast.success('Mensagem copiada — cole no WhatsApp do técnico');
```

Incluir um aviso visível: *"Esta senha aparece só agora. Se perder, gere outra em Resetar senha."*

- [ ] **Step 5: Escrever o dialog de cadastro/edição**

`frontend/dashboard/src/components/technicians/technician-form-dialog.tsx` — campos Nome, CPF (com máscara `000.000.000-00` aplicada no `onChange`), Celular, e um `Switch` "Pode receber equipamentos". No modo edição o CPF fica desabilitado. Ao salvar em modo criação, chama `techniciansApi.create` e devolve o `TechnicianWithPassword` pro pai via callback `onCreated`, que abre o dialog da Step 4.

Seguir o padrão visual de `frontend/dashboard/src/components/stock/associate-stock-dialog.tsx` (mesma estrutura de Dialog, estados de loading e `toast` do sonner).

- [ ] **Step 6: Escrever a página**

`frontend/dashboard/src/app/(dashboard)/tecnicos/page.tsx` — `'use client'`, seguindo a estrutura de [estoque/page.tsx](../../../frontend/dashboard/src/app/(dashboard)/estoque/page.tsx):

- header com ícone `HardHat`, título "Técnicos" e botão "Novo técnico";
- três cards de resumo: total de técnicos ativos, soma de `assignedCount` ("equipamentos em campo"), soma de `installCount` ("instalações");
- busca por nome ou CPF (`techniciansApi.getAll(search)`, com debounce de 400ms);
- lista de `Card`s: nome, CPF formatado, telefone, badges `{assignedCount} em campo` e `{installCount} instalações`, último login (`formatDateBR`) ou "sem login registrado";
- `DropdownMenu` por card: Editar, Ver equipamentos (abre lista via `techniciansApi.assignments`), Resetar senha (confirm + abre o dialog de senha), Ativar/Desativar, Excluir (só ADMIN/SUPER_ADMIN);
- estado vazio com `Card` tracejado igual ao do estoque;
- erro de exclusão com 409 mostra a mensagem do backend via `toast.error`.

- [ ] **Step 7: Verificar no navegador**

```bash
cd frontend/dashboard && npm run dev
```

Abrir `http://localhost:3000/tecnicos`, cadastrar um técnico e conferir: senha aparece, botão de WhatsApp copia o texto certo, o técnico surge na lista com "0 em campo".

- [ ] **Step 8: Build e commit**

```bash
cd frontend/dashboard && npm run build
git add frontend/dashboard/src
git commit -m "feat(tecnicos): tela de cadastro de tecnicos com senha provisoria"
```

---

### Task 7: Frontend — reserva em lote na tela de estoque

**Files:**
- Modify: `frontend/dashboard/src/app/(dashboard)/estoque/page.tsx`
- Modify: `frontend/dashboard/src/types/stock.ts`
- Modify: `frontend/dashboard/src/lib/api.ts`
- Create: `frontend/dashboard/src/components/stock/assign-technician-dialog.tsx`

**Interfaces:**
- Consumes: `stockApi.assign/unassign` (Task 3), `techniciansApi.getAll` (Task 6).
- Produces: `StockItem.assignedTechnician`, `StockItem.assignedAt`.

- [ ] **Step 1: Estender o tipo StockItem**

Em `frontend/dashboard/src/types/stock.ts`, adicionar ao `StockItem`:

```typescript
  assignedTechnician: { id: string; name: string } | null;
  assignedAt: string | null;
```

- [ ] **Step 2: Adicionar as chamadas de API**

Em `frontend/dashboard/src/lib/api.ts`, dentro de `stockApi`:

```typescript
  assign: async (stockItemIds: string[], technicianId: string): Promise<StockAssignResult> => {
    const res = await api.post<ApiResponse<StockAssignResult>>('/stock/assign', { stockItemIds, technicianId });
    return res.data.data;
  },
  unassign: async (stockItemIds: string[]): Promise<StockAssignResult> => {
    const res = await api.post<ApiResponse<StockAssignResult>>('/stock/unassign', { stockItemIds });
    return res.data.data;
  },
```

E em `types/stock.ts`:

```typescript
export interface StockAssignResult {
  ok: number;
  skipped: Array<{ imei: string; motivo: string }>;
}
```

- [ ] **Step 3: Escrever o dialog de reserva**

`frontend/dashboard/src/components/stock/assign-technician-dialog.tsx` — recebe `{ items: StockItem[], open, onOpenChange, onAssigned }`. Carrega `techniciansApi.getAll()` e filtra `active && canReceiveEquipment` no `SelectNative`. Mostra "Enviando N equipamentos para:". Ao confirmar chama `stockApi.assign`, e no retorno:

```typescript
if (res.skipped.length === 0) {
  toast.success(`${res.ok} equipamento(s) reservado(s) para ${nome}`);
} else {
  toast.warning(
    `${res.ok} reservado(s). ${res.skipped.length} não: ` +
      res.skipped.slice(0, 3).map((s) => `${s.imei} (${s.motivo})`).join(', ') +
      (res.skipped.length > 3 ? '…' : ''),
  );
}
```

Se a lista de técnicos vier vazia, mostrar o aviso "Nenhum técnico habilitado a receber equipamentos" com link para `/tecnicos`.

- [ ] **Step 4: Adicionar seleção múltipla na tabela**

Em `frontend/dashboard/src/app/(dashboard)/estoque/page.tsx`:

- estado `const [selected, setSelected] = useState<Set<string>>(new Set())`;
- coluna nova de `<input type="checkbox">` no `<thead>` (marca/desmarca todos os itens da página) e em cada `<tr>`;
- limpar a seleção sempre que `loadStock()` rodar;
- coluna nova "Técnico": mostra `item.assignedTechnician.name` como `Badge` âmbar ou `—`;
- barra flutuante quando `selected.size > 0`, fixa no rodapé:

```tsx
{selected.size > 0 && canManage && (
  <div className="sticky bottom-0 z-10 flex items-center justify-between gap-3 rounded-lg border bg-card px-4 py-3 shadow-lg">
    <span className="text-sm font-medium">{selected.size} selecionado(s)</span>
    <div className="flex gap-2">
      <Button size="sm" variant="outline" onClick={handleUnassign}>
        <Ban className="h-4 w-4 mr-1" /> Cancelar reserva
      </Button>
      <Button size="sm" onClick={() => setAssignOpen(true)}>
        <HardHat className="h-4 w-4 mr-1" /> Enviar pro técnico
      </Button>
    </div>
  </div>
)}
```

- [ ] **Step 5: Adicionar o filtro de reserva**

Ao lado do filtro de status, um `SelectNative` novo com `''` → "Todos", `'free'` → "Livres", `'assigned'` → "Com técnico", enviado como `params.assignment` em `loadStock()`.

- [ ] **Step 6: Ligar o item do menu de 3 pontinhos**

Em `StockRowMenu`, trocar o item "Disponibilizar no login do técnico" (hoje `disabled`) por um item ativo que chama `onAssign(item)` — abre o mesmo dialog com aquele item só.

- [ ] **Step 7: Verificar no navegador**

Com `npm run dev`: marcar 3 itens → "Enviar pro técnico" → escolher → conferir toast, badge do técnico na linha e o filtro "Com técnico" retornando exatamente esses 3.

- [ ] **Step 8: Build e commit**

```bash
cd frontend/dashboard && npm run build
git add frontend/dashboard/src
git commit -m "feat(estoque): selecao em lote e envio de equipamentos pro tecnico"
```

---

### Task 8: Frontend — PWA /tecnico

**Files:**
- Create: `frontend/dashboard/src/app/tecnico/layout.tsx`
- Create: `frontend/dashboard/src/app/tecnico/page.tsx`
- Create: `frontend/dashboard/src/app/tecnico/instalar/[id]/page.tsx`
- Create: `frontend/dashboard/src/lib/tech-api.ts`
- Create: `frontend/dashboard/src/types/tech.ts`

**Interfaces:**
- Consumes: rotas `/tech/*` (Tasks 4 e 5).
- Produces: `techApi` com instância axios própria e token em `localStorage['tech_token']` — separado do token do painel, pra técnico e operador poderem usar o mesmo navegador sem se derrubar.

- [ ] **Step 1: Criar o client isolado**

`frontend/dashboard/src/lib/tech-api.ts`:

```typescript
import axios from 'axios';
import type { TechAssignment, TechMe, TechLoginResponse, TechSignal } from '@/types/tech';
import type { HinovaLookup, StockAssociateResult } from '@/types/stock';

const TOKEN_KEY = 'tech_token';

const techHttp = axios.create({
  baseURL: process.env.NEXT_PUBLIC_API_URL + '/api/v1',
  headers: { 'Content-Type': 'application/json' },
});

techHttp.interceptors.request.use((config) => {
  if (typeof window !== 'undefined') {
    const token = localStorage.getItem(TOKEN_KEY);
    if (token) config.headers.Authorization = `Bearer ${token}`;
  }
  return config;
});

techHttp.interceptors.response.use(
  (r) => r,
  (error) => {
    if (error.response?.status === 401 && typeof window !== 'undefined') {
      localStorage.removeItem(TOKEN_KEY);
      if (!window.location.pathname.startsWith('/tecnico')) return Promise.reject(error);
      window.location.href = '/tecnico';
    }
    return Promise.reject(error);
  },
);

export const techApi = {
  tokenKey: TOKEN_KEY,
  login: async (cpf: string, password: string): Promise<TechLoginResponse> => {
    const res = await techHttp.post('/tech/auth/login', { cpf, password });
    return res.data.data ?? res.data;
  },
  me: async (): Promise<TechMe> => {
    const res = await techHttp.get('/tech/auth/me');
    return res.data.data ?? res.data;
  },
  changePassword: async (currentPassword: string, newPassword: string): Promise<void> => {
    await techHttp.post('/tech/auth/change-password', { currentPassword, newPassword });
  },
  assignments: async (): Promise<TechAssignment[]> => {
    const res = await techHttp.get('/tech/assignments');
    return res.data.data ?? res.data;
  },
  lookup: async (placa: string): Promise<HinovaLookup> => {
    const res = await techHttp.get('/tech/lookup', { params: { placa } });
    return res.data.data ?? res.data;
  },
  signal: async (id: string): Promise<TechSignal> => {
    const res = await techHttp.get(`/tech/assignments/${id}/signal`);
    return res.data.data ?? res.data;
  },
  finish: async (
    id: string,
    payload: { placa: string; installLocation: string; notes?: string },
  ): Promise<StockAssociateResult> => {
    const res = await techHttp.post(`/tech/assignments/${id}/finish`, payload);
    return res.data.data ?? res.data;
  },
};
```

`frontend/dashboard/src/types/tech.ts`:

```typescript
export interface TechMe {
  id: string;
  name: string;
  cpf: string;
  phone: string | null;
  mustChangePassword: boolean;
  tenant: { id: string; name: string; logoUrl: string | null; primaryColor: string | null };
}

export interface TechLoginResponse {
  accessToken: string;
  technician: { id: string; name: string; cpf: string; tenantId: string; mustChangePassword: boolean };
}

export interface TechAssignment {
  id: string;
  imei: string;
  iccid: string | null;
  line: string | null;
  operator: string | null;
  server: string | null;
  status: string | null;
  assignedAt: string | null;
}

export interface TechSignal {
  online: boolean;
  lastUpdate: string | null;
  motivo: string | null;
}
```

- [ ] **Step 2: Criar o layout isolado**

`frontend/dashboard/src/app/tecnico/layout.tsx` — fora do grupo `(dashboard)`, portanto sem sidebar. Fundo navy da marca, container `max-w-md mx-auto`, `min-h-dvh`, e `metadata` com `title: 'Técnico — 21 GO'`. Sem `AuthProvider` do painel: esta área usa o próprio token.

- [ ] **Step 3: Escrever a tela principal**

`frontend/dashboard/src/app/tecnico/page.tsx`, `'use client'`, com três estados na mesma rota:

1. **Sem token** → formulário de login: CPF (máscara) e senha, botão grande "Entrar". Erro → `toast.error('CPF ou senha inválidos')`.
2. **`me.mustChangePassword === true`** → formulário de troca: senha atual (já preenchida com a que ele digitou no login), nova senha, confirmar nova. Só libera com 6+ caracteres e as duas iguais.
3. **Autenticado** → lista de `TechAssignment` em cards grandes (mín. 64px de altura por item, alvo de toque confortável): modelo/IMEI em fonte mono, operadora e linha embaixo, botão "Instalar" que navega pra `/tecnico/instalar/{id}`. Estado vazio: "Nenhum equipamento reservado pra você. Fale com o escritório." Botão "Sair" no topo limpa o `tech_token`.

- [ ] **Step 4: Escrever a tela de instalação**

`frontend/dashboard/src/app/tecnico/instalar/[id]/page.tsx`, `'use client'` (usar `use(params)` do React 19 para os params — confirmar a convenção em `node_modules/next/dist/docs/` conforme `frontend/dashboard/AGENTS.md`):

- topo: IMEI do equipamento + botão voltar;
- campo **Placa** com `value.toUpperCase()` no `onChange` e botão "Buscar no SGA" → `techApi.lookup`;
  - `encontrado: false` → alerta vermelho com `motivo`, botão finalizar bloqueado;
  - `ativo: false` → alerta âmbar "placa está {situacao.descricao} — vínculo bloqueado", botão bloqueado;
  - ok → card verde com nome do cliente, modelo e situação;
- campo **Local de instalação**: `SelectNative` com as opções `Painel`, `Coluna de direção`, `Banco do motorista`, `Porta-luvas`, `Compartimento do motor`, `Outro` — escolhendo "Outro" aparece um `Input` de texto livre obrigatório;
- botão secundário **"Verificar sinal"** → `techApi.signal` → toast verde "rastreador comunicando (última posição há X)" ou âmbar com o `motivo`;
- botão primário **"Finalizar instalação"** → `techApi.finish` → em caso de sucesso, `toast.success('Instalação finalizada — {placa}')` e `router.replace('/tecnico')`;
- erro 422 do SGA mostra a mensagem do backend, sem traduzir.

- [ ] **Step 5: Verificar no navegador em viewport de celular**

Com `npm run dev`, abrir `http://localhost:3000/tecnico` em viewport 390×844 (DevTools). Logar com o técnico da Task 4, trocar a senha, ver a lista, instalar um equipamento com placa real do SGA e conferir que ele some da lista.

- [ ] **Step 6: Build e commit**

```bash
cd frontend/dashboard && npm run build
git add frontend/dashboard/src
git commit -m "feat(tecnicos): PWA /tecnico para finalizar instalacao em campo"
```

---

### Task 9: Deploy em produção

**Files:**
- Nenhum arquivo de código. Execução no droplet `167.71.31.77` via SSH.

**Interfaces:**
- Consumes: tudo das Tasks 1–8, já commitado.

**ATENÇÃO — REGRA 0:** builds no droplet são **sequenciais**. Backend e frontend ao mesmo tempo estouram a memória e derrubam containers em produção.

- [ ] **Step 1: Baseline — provar que produção está de pé ANTES de mexer**

```bash
curl -s -o /dev/null -w "dashboard:%{http_code}\n" https://trackgo.site/login
curl -s -o /dev/null -w "api:%{http_code}\n" https://api.trackgo.site/api/v1/health
```

Esperado: `200` nos dois. Se algum não estiver 200, **parar** e resolver antes de qualquer deploy.

- [ ] **Step 2: Push do código**

```bash
git push origin main
```

- [ ] **Step 3: Aplicar a migration em produção (aditiva, via DIRECT_URL)**

```bash
ssh -i ~/.ssh/claude_21go root@167.71.31.77 \
  "docker exec \$(docker ps -q -f name=backend-rastreamento | head -1) npx prisma migrate deploy"
```

Esperado: `Applying migration 20260721120000_add_technicians` ou `No pending migrations`. Se falhar por permissão, aplicar o `migration.sql` da Task 1 direto via `psql` no container do postgres — o DDL é idempotente (`IF NOT EXISTS`).

- [ ] **Step 4: Build do backend (sozinho) e push pro registry**

```bash
ssh -i ~/.ssh/claude_21go root@167.71.31.77 "cd /root/21go && git pull && \
  docker build -t localhost:5000/r21go-backend:latest -f backend/Dockerfile backend/ && \
  docker push localhost:5000/r21go-backend:latest && \
  docker service update --force --image localhost:5000/r21go-backend:latest backend-rastreamento_backend"
```

Aguardar terminar completamente antes do próximo passo.

- [ ] **Step 5: Verificar o backend em produção**

```bash
curl -s https://api.trackgo.site/api/v1/health
curl -s -o /dev/null -w "%{http_code}\n" -X POST https://api.trackgo.site/api/v1/tech/auth/login \
  -H "Content-Type: application/json" -d '{"cpf":"00000000000","password":"xxxxxx"}'
```

Esperado: health `200` com `gitSha` novo; login `401` (rota existe e responde — 404 significaria que o módulo não subiu).

- [ ] **Step 6: Build do frontend (só depois do backend terminar) com os build args obrigatórios**

```bash
ssh -i ~/.ssh/claude_21go root@167.71.31.77 "cd /root/21go && \
  docker build -t localhost:5000/r21go-frontend:latest \
  --build-arg NEXT_PUBLIC_API_URL=https://api.trackgo.site \
  --build-arg NEXT_PUBLIC_WS_URL=wss://api.trackgo.site \
  --build-arg NEXT_PUBLIC_TRACCAR_URL=https://traccar.trackgo.site \
  -f frontend/dashboard/Dockerfile frontend/dashboard/ && \
  docker push localhost:5000/r21go-frontend:latest && \
  docker service update --force --image localhost:5000/r21go-frontend:latest frontend-rastreamento_frontend"
```

Faltar qualquer um dos build args deixa a URL da API relativa e quebra tudo em produção.

- [ ] **Step 7: Verificação final obrigatória**

```bash
curl -s -o /dev/null -w "dashboard:%{http_code}\n" https://trackgo.site/login
curl -s -o /dev/null -w "tecnicos:%{http_code}\n" https://trackgo.site/tecnicos
curl -s -o /dev/null -w "pwa:%{http_code}\n" https://trackgo.site/tecnico
curl -s -o /dev/null -w "api:%{http_code}\n" https://api.trackgo.site/api/v1/health
```

Esperado: `200` nos quatro.

- [ ] **Step 8: Teste de fumaça com dado real**

No painel em produção: cadastrar um técnico de teste, reservar 1 equipamento, abrir `/tecnico` no celular, logar, trocar a senha e conferir que o equipamento aparece na lista. **Não finalizar** a instalação com placa real de cliente — usar uma placa de teste ou parar antes do botão finalizar.

- [ ] **Step 9: Atualizar a documentação do projeto**

Em `CLAUDE.md`, na tabela "O que já funciona hoje", adicionar a linha:

```markdown
| Técnicos (cadastro, reserva de equipamento, PWA de campo) | ✓ | [technicians.service.ts](backend/src/modules/technicians/technicians.service.ts), [tecnico/page.tsx](frontend/dashboard/src/app/tecnico/page.tsx) |
```

- [ ] **Step 10: Commit final**

```bash
git add CLAUDE.md
git commit -m "docs: registra modulo de tecnicos como funcional"
git push origin main
```

---

## Rollback

Se algo quebrar em produção depois do deploy:

```bash
ssh -i ~/.ssh/claude_21go root@167.71.31.77 \
  "docker service rollback backend-rastreamento_backend && docker service rollback frontend-rastreamento_frontend"
```

A migration é aditiva — as colunas novas ficam no banco sem quebrar a versão anterior do código, então **não é preciso reverter o banco**.
